export const searchFilterSelector = (state) => state.filter.search;
export const categoryFilterSelector = (state) => state.filter.category;
export const sortFilterSelector = (state) => state.filter.sort;

export const navModelProductSelector = (state) => state.navModel.product;
export const navModelIsOpenSelector = (state) => state.navModel.isOpen;
